import FooterButtonAndLink from '.';

export default {
  title: 'FooterButtonAndLink',
  component: FooterButtonAndLink,
};

const Template = (args: any) => <FooterButtonAndLink {...args} />;

export const Default: any = Template.bind({});
Default.args = {};
