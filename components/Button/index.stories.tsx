import ButtonDate from '.';

export default {
  title: 'ButtonDate',
  component: ButtonDate,
};

const Template = (args: any) => <ButtonDate {...args} />;

export const Default: any = Template.bind({});
Default.args = {};
