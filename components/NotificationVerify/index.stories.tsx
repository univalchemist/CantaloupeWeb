import NotificationVerify from '.';

export default {
  title: 'NotificationVerify',
  component: NotificationVerify,
};

const Template = (args: any) => <NotificationVerify {...args} />;

export const Default: any = Template.bind({});
Default.args = {};
