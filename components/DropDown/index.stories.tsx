import DropDown from '.';

export default {
  title: 'DropDown',
  component: DropDown,
};

const Template = (args: any) => <DropDown {...args} />;

export const Default: any = Template.bind({});
Default.args = {};
