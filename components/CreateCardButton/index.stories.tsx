import CreateCardButton from '.';

export default {
  title: 'CreateCardButton',
  component: CreateCardButton,
};

const Template = (args: any) => <CreateCardButton {...args} />;

export const Default: any = Template.bind({});
Default.args = {};
