import Click2PayCheckoutBox from '.';

export default {
  title: 'Click2PayCheckoutBox',
  component: Click2PayCheckoutBox,
};

const Template = (args: any) => <Click2PayCheckoutBox {...args} />;

export const Default: any = Template.bind({});
Default.args = {};
