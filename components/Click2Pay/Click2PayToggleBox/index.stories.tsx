import Click2PayToggleBox from '.';

export default {
  title: 'Click2PayToggleBox',
  component: Click2PayToggleBox,
};

const Template = (args: any) => <Click2PayToggleBox {...args} />;

export const Default: any = Template.bind({});
Default.args = {};
