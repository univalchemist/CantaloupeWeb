import OTPChannelSelection from '.';

export default {
  title: 'OTPChannelSelection',
  component: OTPChannelSelection,
};

const Template = (args: any) => <OTPChannelSelection {...args} />;

export const Default: any = Template.bind({});
Default.args = {};
