import SpacerContainer from '.';

export default {
  title: 'SpacerContainer',
  component: SpacerContainer,
};

const Template = (args: any) => <SpacerContainer {...args} />;

export const Default: any = Template.bind({});
Default.args = {};
