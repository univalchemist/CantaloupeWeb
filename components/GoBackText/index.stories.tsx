import GoBackText from '.';

export default {
  title: 'GoBackText',
  component: GoBackText,
};

const Template = (args: any) => <GoBackText {...args} />;

export const Default: any = Template.bind({});
Default.args = {};
