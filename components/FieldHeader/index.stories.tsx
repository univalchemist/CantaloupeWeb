import FieldHeader from '.';

export default {
  title: 'FieldHeader',
  component: FieldHeader,
};

const Template = (args: any) => <FieldHeader {...args} />;

export const Default: any = Template.bind({});
Default.args = {};
