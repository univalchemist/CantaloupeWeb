import CardAutoReloadBox from '.';

export default {
  title: 'CardAutoReloadBox',
  component: CardAutoReloadBox,
};

const Template = (args: any) => <CardAutoReloadBox {...args} />;

export const Default: any = Template.bind({});
Default.args = {};
