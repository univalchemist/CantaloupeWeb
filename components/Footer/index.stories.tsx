import Footer from '.';

export default {
  title: 'Footer',
  component: Footer,
};

const Template = (args: any) => <Footer {...args} />;

export const Default: any = Template.bind({});
Default.args = {};
