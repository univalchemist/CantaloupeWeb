import ChangeRouteText from '.';

export default {
  title: 'ChangeRouteText',
  component: ChangeRouteText,
};

const Template = (args: any) => <ChangeRouteText {...args} />;

export const Default: any = Template.bind({});
Default.args = {};
