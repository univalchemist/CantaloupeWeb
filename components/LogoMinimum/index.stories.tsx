import LogoMinimum from '.';

export default {
  title: 'LogoMinimum',
  component: LogoMinimum,
};

const Template = (args: any) => <LogoMinimum {...args} />;

export const Default: any = Template.bind({});
Default.args = {};
