import BackButton from '.';

export default {
  title: 'BackButton',
  component: BackButton,
};

const Template = (args: any) => <BackButton {...args} />;

export const Default: any = Template.bind({});
Default.args = {};
