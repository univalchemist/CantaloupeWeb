import AddItemIcon from '.';

export default {
  title: 'AddItemIcon',
  component: AddItemIcon,
};

const Template = (args: any) => <AddItemIcon {...args} />;

export const Default: any = Template.bind({});
Default.args = {};
