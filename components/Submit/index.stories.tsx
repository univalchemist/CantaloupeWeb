import Submit from '.';

export default {
  title: 'Submit',
  component: Submit,
};

const Template = (args: any) => <Submit {...args} />;

export const Default: any = Template.bind({});
Default.args = {};
