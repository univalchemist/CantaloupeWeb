import ApolloErrorHandling from '.';

export default {
  title: 'ApolloErrorHandling',
  component: ApolloErrorHandling,
};

const Template = (args: any) => <ApolloErrorHandling {...args} />;

export const Default: any = Template.bind({});
Default.args = {};
