import ToggleSwitch from '.';

export default {
  title: 'ToggleSwitch',
  component: ToggleSwitch,
};

const Template = (args: any) => <ToggleSwitch {...args} />;

export const Default: any = Template.bind({});
Default.args = {};
