import NavbarAndFooterLink from '.';

export default {
  title: 'NavbarAndFooterLink',
  component: NavbarAndFooterLink,
};

const Template = (args: any) => <NavbarAndFooterLink {...args} />;

export const Default: any = Template.bind({});
Default.args = {};
