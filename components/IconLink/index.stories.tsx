import IconLink from '.';

export default {
  title: 'IconLink',
  component: IconLink,
};

const Template = (args: any) => <IconLink {...args} />;

export const Default: any = Template.bind({});
Default.args = {};
