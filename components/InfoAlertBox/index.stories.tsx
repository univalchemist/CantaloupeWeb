import InfoAlertBox from '.';

export default {
  title: 'InfoAlertBox',
  component: InfoAlertBox,
};

const Template = (args: any) => <InfoAlertBox {...args} />;

export const Default: any = Template.bind({});
Default.args = {};
