import CardAutoReloadSettings from '.';

export default {
  title: 'CardAutoReloadSettings',
  component: CardAutoReloadSettings,
};

const Template = (args: any) => <CardAutoReloadSettings {...args} />;

export const Default: any = Template.bind({});
Default.args = {};
