import FormInputReadOnly from '.';

export default {
  title: 'FormInputReadOnly',
  component: FormInputReadOnly,
};

const Template = (args: any) => <FormInputReadOnly {...args} />;

export const Default: any = Template.bind({});
Default.args = {};
