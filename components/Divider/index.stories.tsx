import Divider from '.';

export default {
  title: 'Divider',
  component: Divider,
};

const Template = (args: any) => <Divider {...args} />;

export const Default: any = Template.bind({});
Default.args = {};
